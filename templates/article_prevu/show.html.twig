{% extends 'base.html.twig' %}

{% block title %}ArticlePrevu{% endblock %}

{% block body %}
    <h1>ArticlePrevu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_prevu.id }}</td>
            </tr>
            <tr>
                <th>Quantité</th>
                <td>{{ article_prevu.Quantite }}</td>
            </tr>
            <tr>
                <th>StatutAchat</th>
                <td>{{ article_prevu.StatutAchat ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ nom_article_prevu.nom_article }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_prevu_index') }}">back to list</a>

    <a href="{{ path('app_article_prevu_edit', {'id': article_prevu.id}) }}">edit</a>

    {{ include('article_prevu/_delete_form.html.twig') }}
{% endblock %}
