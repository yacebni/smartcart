{% extends 'base.html.twig' %}

{% block title %}ArticlePrevu index{% endblock %}

{% block body %}
    <h1>ArticlePrevu index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantit�e</th>
                <th>StatutAchat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article_prevu in article_prevus %}
            <tr>
                <td>{{ article_prevu.id }}</td>
                <td>{{ article_prevu.Quantite }}</td>
                <td>{{ article_prevu.StatutAchat ? 'Yes' : 'No' }}</td>
                <td>
                    <a href="{{ path('app_article_prevu_show', {'id': article_prevu.id}) }}">show</a>
                    <a href="{{ path('app_article_prevu_edit', {'id': article_prevu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_article_prevu_new') }}">Create new</a>
{% endblock %}
