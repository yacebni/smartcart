<form class="rounded-xl border border-red-600 mt-8 text-red-600 font-bold px-3 py-2 hover:opacity-70 duration-300 inline-block" method="post" action="{{ path('app_type_article_delete', {'id': type_article.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_article.id) }}">
    <button class="btn">Supprimer le type</button>
</form>
